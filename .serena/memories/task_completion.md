# Task Completion

- Review `git status --short --branch`, diff stat, and actual diff; preserve unrelated changes.
- Run `JAVA_HOME=/Applications/PyCharm.app/Contents/jbr/Contents/Home ./gradlew test buildPlugin verifyPlugin --console=plain`.
- Confirm descriptor does not impersonate vendor/product names and no source uses force-close, auto-save, process-signal, or broad-kill behavior.
- Inspect staged paths for secrets/private data before committing.